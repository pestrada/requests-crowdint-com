%h1 A new request has been submitted!
%br/
%blockquote "#{@request.description}"
%p 
  If you want this to be done go to 
  = link_to root_url, root_url + "#requests/#{@request.id}" 
  and support it by giving it a 'like'.
%p 
  = ENV['VOTES_LIMIT']
  likes are needed for the request to be taken into consideration.