- cat = "{{ this.mood }}"
.mood
  = image_tag "icons/#{cat}.png"
.description
  %p
    {{ this.description }}
  .info
    .category-name
      {{ this.mood }}
    .time
      {{ this.created_at }}
    .owner
      {{#remove_domain}}{{this.email}}{{/remove_domain}}

.vote-controls
  .votes
    %span {{ this.votes }}
    %span VOTES
  = link_to '+1', '#', class: 'like'
