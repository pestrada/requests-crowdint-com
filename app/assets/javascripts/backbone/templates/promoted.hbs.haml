.description
  %p
    {{ this.description }}
.state
  .date
    {{ this.created_at }}
  .mood-state
    {{ this.mood }}
  %div{ class: 'promoted' }
  {{#if this.isAdmin}}
  .actions
    = link_to 'accept', '#', {class: 'accept'}
    = link_to 'reject', '#', {class: 'reject'}
  {{/if}}
