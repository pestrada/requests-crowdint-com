.description
  %p
    {{ this.description }}
.state
  .date
    20/12/13
  .mood-state
    {{ this.mood }}
  %div{ class: 'accepted' }
  {{#if this.isAdmin}}
  .actions
    = link_to 'Complete', '#', {class: 'complete'}
  {{/if}}
